# Server
PORT=3000

# MongoDB connection
# Plain URI with host/port. Credentials are optional if included here.
MONGODB_URI=mongodb://localhost:27017/petcare

# If your MongoDB server enforces authentication, set the username/password.
# Leave blank if the URI already includes credentials.
MONGODB_USER=petcare
MONGODB_PASS=super-secret-password

# Optional: provide the database name explicitly when using authentication.
MONGODB_DB_NAME=petcare

# Optional: authentication database (commonly "admin").
MONGODB_AUTH_SOURCE=admin

# Optional: replica set name.
# MONGODB_REPLICA_SET=rs0

# Optional: enable TLS by setting to true/1 if your MongoDB requires it.
# MONGODB_TLS=false
